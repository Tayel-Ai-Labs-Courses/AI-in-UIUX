# Submissions

One folder per student, named in lowercase with hyphens: `ahmed-hassan`.

Start by copying the template:

```bash
../scripts/new-submission.sh "ahmed-hassan"
```

Work only inside your own folder. Do not edit anyone else's.

`_TEMPLATE/` is the shared starting point — leave it alone.
